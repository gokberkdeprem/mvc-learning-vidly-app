@model IEnumerable<Vidly.Models.Customer>
@{
    /**/

    ViewBag.Title = "Index";
}
<h2>Customers</h2>

<table id="customers" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Customer</th>
            <th>Membership Type</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section scripts
{
    <script>
        $(document).ready(function () {
            //feeding data table using customer's api
            $("#customers").DataTable({
                ajax: {
                    url: "/api/customers",
                    dataSrc: "" // we don't need to give customer because it is an array -> check [1] at the bottom
                },
                columns: [
                    {
                        data: "name",
                        render: function (data, type, customer) {
                            return "<a href='/customers/edit/" + customer.ID + "'>" + customer.name + "</a>";
                        }
                    },
                    {
                        data: "membershipType.name"
                    },
                    {
                        data: "id",
                        render: function (data) {
                            return "<button class='btn-link js-delete' data-customer-id=" + data + ">Delete</button>";
                        }
                    }
                ]
            });

            //when we moved the js-delete class from next to-customers to inside on click event we optimize our code
            //Instead of creating onclick event for every element we created one and a js-delete hook
            $("#customers").on("click", ".js-delete", function () {
                var button = $(this);
                bootbox.confirm("Are you sure you want to delete this customer ?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "api/customers/" + button.attr("data-customer-id"),
                            method: "DELETE",
                            success: function () {
                                button.parents("tr").remove();
                            }
                        })
                    }
                });
            });
        });
    </script>

}

@*[1] If our api were like the code below, we should add Data source "customer" object but our array is like the second one
    so we need to keep it blank

   a) {
        count : 10,
        customers : [
            {...},
            {...},
            ...
    ]
    }

    b)[
      {...},
      {...},
       ...
    ]

*@
